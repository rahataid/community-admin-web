import { CommunityCreateFormView } from '@sections/community/add';

export const metadata = {
  title: 'Community: Create a new Community',
};

export default function UserCreatePage() {
  return <CommunityCreateFormView />;
}
